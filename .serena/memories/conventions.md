# Conventions

- **Code Style**: Follows standard Python guidelines (PEP 8).
- **Type Hints**: Used throughout the codebase (based on Typer usage).
- **Configuration**: Uses `.env` for secrets (e.g., `GOOGLE_API_KEY`).