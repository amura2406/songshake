# ==============================
# Song Shake — Production Environment
# ==============================
# Copy to .env and fill in values before deploying.
# For Cloud Run, these are set via --set-env-vars and --update-secrets in deploy.sh.

# --- Storage ---
STORAGE_BACKEND=firestore

# --- Runtime ---
ENV=production
LOG_LEVEL=INFO

# --- Google / Gemini API Key ---
# Used for AI enrichment (Gemini)
GOOGLE_API_KEY=

# --- Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_REDIRECT_URI=https://songshake999.web.app/auth/google/callback

# --- JWT ---
# REQUIRED in production.
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=

# --- CORS ---
# Lock to your Firebase Hosting domain
CORS_ORIGINS=https://songshake999.web.app

# --- Frontend URL (for OAuth redirect back to SPA) ---
FRONTEND_URL=https://songshake999.web.app
